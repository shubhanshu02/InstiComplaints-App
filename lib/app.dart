import 'package:flutter/material.dart';
import 'st_profile.dart';
import 'camera1.dart';

class InstiComplaints extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'InstiComplaints',
      home: Profile(),
    );
  }
}
